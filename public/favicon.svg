<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Dark navy background with rounded corners -->
  <rect width="32" height="32" rx="7" fill="#000079"/>
  <!-- Sky-blue circle (left) -->
  <circle cx="11" cy="16" r="7" fill="#00b0ff" opacity="0.92"/>
  <!-- Fresh-green circle (right) -->
  <circle cx="21" cy="16" r="7" fill="#42db66" opacity="0.92"/>
  <!-- Hot-pink circle (centre, frontmost — slightly larger so it dominates) -->
  <circle cx="16" cy="16" r="7.5" fill="#ff0065"/>
</svg>
